# Cloudflare Pages headers (for static export)
# These headers are applied at the edge, not by Next.js

/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()

# Production-only headers (HSTS, CSP)
# Note: Cloudflare Pages doesn't support conditional headers in _headers file
# These should be set via Cloudflare Dashboard → Rules → Transform Rules → Modify Response Header
# Or via wrangler.toml if using Cloudflare Workers

