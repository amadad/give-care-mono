/**
 * Legacy shim
 *
 * The Twilio Convex Component registers its own `internal.twilio` namespace,
 * so we no longer export helpers from this file to avoid path conflicts.
 * See `convex/internal/sms.ts` for the actual implementations.
 */
